{
  "Logging": {
    "LogLevel": {
      "Default": "Information",
      "Microsoft.AspNetCore": "Warning"
    }
  },
  "AllowedHosts": "*",
  "Urls": "http://localhost:5000;https://localhost:5001",
  "AppSettings": {
    "Storage": {
      "Type": "File",
      "Description": "Options: File (encrypted JSON in AppData) or InMemory (for testing)"
    }
  }
}
